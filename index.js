let = 'hello'
console.log(let);

const weight = prompt ('please enter your weight in kg');
const height = prompt ('please enter your weight in meter');

const BMI = weight / (height * height);

console.log(`Your BMI is ${BMI}`);
